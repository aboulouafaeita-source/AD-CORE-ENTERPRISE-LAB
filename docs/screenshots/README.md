# Screenshots

This folder contains validation screenshots for the AD + ASA + EVE-NG lab.

## Topology images

- `topology-diagram.png`  
  Clean topology diagram created separately for documentation.

- `topology-eve-ng.png`  
  Real implementation screenshot from EVE-NG.

## Validation screenshots

- `asa-connectivity.png`  
  Confirms ASA interface and connectivity validation.

- `server-core-domain.png`  
  Confirms Windows Server Core domain controller status.

- `user1-enabled.png`  
  Confirms that the Active Directory user account `user1` exists and is enabled.

- `domain-login-validation.png`  
  Confirms successful login from the domain-joined client.

- `dns-resolution.png`  
  Confirms successful DNS name resolution for the domain and domain controller.

## Notes

These screenshots are included as supporting evidence that the lab was implemented successfully.

The repository does not include operating system images, Cisco images, or any licensed VM files.
